import { Custom1Directive } from './custom1.directive';

describe('Custom1Directive', () => {
  it('should create an instance', () => {
    const directive = new Custom1Directive();
    expect(directive).toBeTruthy();
  });
});
